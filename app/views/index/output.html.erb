<h1>Результат</h1>
<% if @error%>
   <p><%= @error %></p>
<% else %>

   <h4>Последний элемент таблицы будет результатом вычислений</h4>
   <table >
   <tr>
     <th>Номер элемента</th>
     <th>Значение</th>
   </tr>

   <% @mas.each_with_index do |r, idx|%>

   <tr>
     <td><%= idx+1 %></td>
     <td><%= r %></td>
   </tr>

   <% end %>
   </table>
<% end %>

<%= link_to "Back to input", index_input_url %>